entity Test:
    string attr
end


Test(attr="a")


typedef my_symbol_test_type as string matching /[a-zA-Z]*/
my_symbol_test_value = 0


import testmodule

# don't add anything above this line to prevent line number failures in test case
